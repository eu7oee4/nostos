# Required — your own key (BYOK)
LLM_API_KEY=

# Optional
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
DATA_DIR=./data
PORT=8787
# Self-host single user
USER_ID=local

# Proactive outreach: off by default (channel undecided)
PROACTIVE_ENABLED=false
